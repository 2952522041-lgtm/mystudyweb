# PDF 随页翻译学习软件——产品设计文档

## 1. 产品概述

本产品是一款面向外语学习和资料阅读场景的 PDF 阅读工具。

用户导入外文 PDF 后，可以在左侧阅读原文。当用户滚动到某一页时，右侧自动显示该页的中文翻译，减少复制、粘贴和切换翻译工具带来的打断，提供连续、专注的双语阅读体验。

第一阶段以 MVP 形式验证核心需求，暂不追求复杂的学习功能和完整的 PDF 排版还原。

## 2. 产品目标

MVP 主要验证以下几点：

- 用户是否需要“原文与译文并排阅读”
- 随页自动翻译是否比手动复制翻译更方便
- 页面跟随、翻译速度和译文质量是否足以支持持续阅读
- 用户是否愿意重复使用该产品阅读教材、论文或电子书

核心体验可以概括为：

> 导入 PDF，阅读到哪一页，译文就跟到哪一页。

## 3. 目标用户

主要面向需要阅读外文 PDF 的用户，包括：

- 阅读英文教材和论文的学生
- 阅读外文技术文档的开发者和专业人士
- 使用外语资料进行学习的用户
- 希望通过原文与译文对照提高阅读能力的语言学习者

## 4. 典型使用场景

### 场景一：阅读英文教材

用户导入一本英文教材，在左侧阅读原文。滚动到新页面后，右侧自动显示对应的中文翻译。

### 场景二：阅读论文

用户阅读英文论文时，通过右侧译文快速理解复杂段落，同时保留原文作为参考。

### 场景三：继续上次阅读

用户重新打开之前导入的 PDF，软件自动恢复上次阅读页码，并显示已经缓存的译文。

## 5. MVP 功能范围

### 5.1 PDF 导入

- 支持用户从本地导入 PDF
- 显示文件名称、总页数和加载状态
- 对文件格式和大小进行基本检查
- 识别 PDF 是否包含可提取文字
- 对暂不支持的扫描版 PDF 给出明确提示

### 5.2 PDF 阅读

- 左侧显示原始 PDF
- 支持连续滚动
- 支持上一页、下一页和指定页码跳转
- 支持放大和缩小
- 显示当前页码和总页数

### 5.3 随页翻译

- 判断用户当前主要阅读的页面
- 自动提取当前页文字
- 将当前页内容翻译为目标语言
- 右侧显示与当前页对应的译文
- 用户快速滚动时延迟触发翻译，避免产生无效请求
- 可提前加载下一页翻译，减少等待时间

### 5.4 翻译结果展示

- 按段落展示译文
- 尽量保持原文的阅读顺序和基本换行
- 显示翻译中、翻译完成和翻译失败状态
- 支持手动重新翻译当前页
- 支持复制译文

MVP 不要求译文完全复刻 PDF 的复杂版式、表格和图片位置。

### 5.5 翻译设置

- 默认自动识别原文语言
- 默认翻译为简体中文
- 支持切换目标语言
- 记录用户最近使用的语言设置

### 5.6 阅读记录与缓存

- 保存上次阅读页码
- 保存已经翻译的页面
- 再次访问已翻译页面时直接显示缓存
- 保存左右面板宽度和阅读设置
- 重新打开同一文件时恢复阅读进度

MVP 阶段可以只保存在用户本地，暂不提供账号和云同步。

## 6. 页面结构

产品主要由一个阅读页面构成。

### 顶部工具栏

包括：

- 文件名称
- 导入或更换 PDF
- 当前页码和总页数
- 原文语言和目标语言
- 缩放控制

### 左侧原文区

用于显示和滚动阅读原始 PDF。

### 右侧翻译区

用于显示当前页译文，包括翻译状态、复制和重新翻译操作。

### 底部状态区

可显示当前翻译状态、缓存状态和错误提示。

左右区域之间支持拖动调整宽度，翻译区域也可以临时折叠。

## 7. 核心操作流程

1. 用户进入软件。
2. 用户选择并导入一份 PDF。
3. 系统解析文件并显示第一页。
4. 系统提取第一页文字并开始翻译。
5. 右侧显示第一页译文。
6. 用户向下滚动 PDF。
7. 系统识别新的当前页并显示对应译文。
8. 已翻译页面自动保存到缓存。
9. 用户下次打开同一 PDF 时恢复阅读位置。

## 8. MVP 暂不包含的功能

以下功能不进入第一版：

- 扫描版 PDF 的 OCR
- 完整还原译文版式
- 导出双语 PDF
- AI 全文问答
- 自动生成全文摘要
- 划词翻译
- 高亮和批注
- 学习笔记
- 单词本和记忆卡片
- 用户账号
- 云端同步
- 多人协作
- 手机端深度适配

这些功能可以在核心阅读体验得到验证后逐步加入。

## 9. 体验要求

MVP 应重点保证：

- PDF 导入过程简单明确
- 当前页判断准确
- 原文与译文页面对应正确
- 快速滚动不会频繁触发无效翻译
- 已翻译页面切换时能够立即显示
- 翻译失败时提供清晰原因和重试入口
- 用户的 PDF 内容和翻译数据得到适当保护

## 10. MVP 验收标准

满足以下条件即可认为 MVP 基本完成：

- 用户可以成功导入文字型 PDF
- PDF 能够正常滚动、翻页和缩放
- 当前页面变化后，右侧能够自动显示对应译文
- 翻译过程有明确的加载和错误状态
- 已翻译内容能够缓存
- 重新打开 PDF 后能够恢复阅读进度
- 核心流程在常见英文教材、论文和电子书上可以正常使用

## 11. 后续发展方向

### 第二阶段：增强阅读体验

- 划词翻译
- 原文和译文段落对应
- 高亮与笔记
- 搜索原文和译文

### 第三阶段：加入 AI 学习能力

- 当前页总结
- 章节总结
- 针对 PDF 内容提问
- 自动提取重点和生词
- 生成记忆卡片

### 第四阶段：完善产品能力

- 用户账号
- 云端文件和阅读记录同步
- 扫描 PDF 的 OCR
- 多端支持
- 双语文档导出

## 12. 产品成功指标

MVP 发布后主要关注：

- 成功导入 PDF 的用户比例
- 成功获得第一页译文的用户比例
- 平均阅读页数
- 单次阅读时长
- 已翻译页面的再次访问比例
- 用户第二次打开产品的比例
- 翻译失败率和页面匹配错误率

MVP 的首要成功标准不是功能数量，而是用户能够顺畅完成“导入 PDF—阅读原文—查看随页译文—持续翻页”这一核心过程。
